document.querySelector(".initGame").addEventListener("click", createMainMenu);











































//=================================Testing if functions would work===========================================
/*var gameArray = [["multi", 5], [0]];

var testarray = function (gameArray) {
    if (gameArray[0] == "multi") {
        consologging(gameArray[1]);
    }
    if (gameArray[0] == false) {
        console.log("do nothing")
    }
}

var consologging = function (numTimes) {
    var numa = 0;
    for (i = 0; i < numTimes; i++) {
        numa++
        console.log(numa);
    }
}*/